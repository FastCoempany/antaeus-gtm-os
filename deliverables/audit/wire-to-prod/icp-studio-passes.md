# ICP Studio — wire-to-production passes (2026-07-09)

Settled design: Combo · 1 Flow (canon §4.4). Surface:
`src/icp-studio/v4/IcpStudioV4.tsx` + scoped CSS. Flag: default ON;
kill-switch `room_icp_studio_v4_off`; hatches `?v4=0/1`.

## What shipped
- The one definition assembling live as a serif statement (all 7 pieces
  present = complete — checked piece by piece, never the placeholder
  text's truthiness); the sharpness ladder (broad → forming → workable
  → sharp) from the shipped 8-check quality engine, warn tones amber.
- **Its bite**: the real watched accounts scored against the definition
  via the shipped `scoreAccountAgainstIcp` — the fit/loose/kept-out
  segment bar + example rows, with "kept out — that's the point."
- The 7-piece builder; the founder/first-seller role toggle (drives the
  shipped role-aware focus + overage checks); buying-group minimum;
  focus recommendation; the flows-downstream line; save through the
  shipped `saveDraftAsIcp`. GroundLine mounted.

## Passes
Pass 1: ICP as a strategic bet, not a persona form — the definition is
central, the builder supports it; ICP-Match visibly biting on real
accounts. Pass 2: voice gate green; no worksheet energy (the statement
leads). Pass 3: boot-verified headless with seeded accounts; the
Wave-B reviewer's findings (role control missing, warn/risk conflated)
fixed same day; zero pageerrors.
