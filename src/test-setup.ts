/**
 * Global test setup for Vitest.
 *
 * Extend this file as testing needs grow (e.g., global mocks, testing-library
 * matchers, MSW handlers). Kept minimal for Phase 1; grows in Phase 1.2 when
 * the testing infrastructure subphase is exercised against real components.
 */
